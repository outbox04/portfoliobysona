// Vercel Web Analytics - Client-side initialization
// This file is kept for potential custom analytics events in the future
// The main analytics tracking is handled by the Vercel script in the HTML templates

// Example of tracking custom events (uncomment and modify as needed):
/*
window.va = window.va || function () { 
  (window.vaq = window.vaq || []).push(arguments); 
};

// Track custom event
// va('event', { name: 'custom_event_name' });
*/

console.log('Vercel Web Analytics loaded');
